# SKILLS INDEX — auto-generated by tools/skills-index · regenerate after S2/S3/S4
# status | name | audit | trigger (≤160chars)
equipped	a11y-audit	AUDIT-PASS	Use when auditing agent-built HTML dashboards or UI for accessibility — WCAG 2.2 AA contrast, keyboard navigation, focus visibility, target size, screen-reader 
equipped	agent-run-contract	AUDIT-PASS	Use when orchestrating headless agent runs (cron, batch, AFK loops) — designing completion signals, structured-output tags, iteration/timeout policy, or prompt 
equipped	ai-engineering-loop	AUDIT-PASS	Runs one epoch cycle of a continuous multi-squad AI engineering workflow (Researcher, Engineer, QA-Tester, UX-UI-Designer squads, each led by a PM agent) in Cla
equipped	apple-silicon-swift	AUDIT-PASS	Use when writing, compiling, or distributing Swift on this Apple Silicon (M1, CLT-only) Mac — choosing zero-dep single-file builds vs frameworks, using the Foun
equipped	canvas-design	AUDIT-PASS	Create beautiful visual art in .png and .pdf documents using design philosophy. You should use this skill when the user asks to create a poster, piece of art, d
equipped	code-review	AUDIT-PASS	Use when asked to review a diff since a fixed point (commit, branch, tag, HEAD~N), a swarm build lane's output, or a PR before merge. Two-axis review run as par
equipped	delegate-offload	AUDIT-PASS	Assess an incoming task and, when it's well-scoped enough to delegate, offload it to a cheaper worker model (rotating across cline-pass / OpenCode Go / OpenCode
equipped	design-review	AUDIT-PASS	Use when reviewing or scoring agent-built UI — a dashboard, admin screen, or HTML artifact — before it ships. Scores 6 weighted dimensions, applies Nielsen's 10
equipped	diagnosing-bugs	AUDIT-PASS	Use when something is broken, failing, throwing, or slow and the cause is not obvious: a Swift dashboard build/runtime failure, an n8n workflow misbehaving, a c
equipped	diagram-design	AUDIT-PASS	Create branded architecture, IT current-state, flowchart, sequence, state machine, ER/data model, timeline, swimlane, quadrant, radar/spider, polar chart (polar
equipped	fact-check	AUDIT-PASS	Data-first news fact-checking and authentic-review finding — verifies claims against hard data and independent corroboration, and cuts past marketing/SEO (inclu
equipped	frontend-design	AUDIT-PASS	Guidance for distinctive, intentional visual design when building new UI or reshaping an existing one. Helps with aesthetic direction, typography, and making ch
equipped	handoff-brief	AUDIT-PASS	Use when ending a session so another agent can continue, swapping a swarm lane under a DELEGATION-CONTRACT, or after a hard-gate rejection that demands a fresh 
equipped	handoff-doc	AUDIT-PASS	Structure content for a technical/project handoff document — the kind given to engineers, AI coding agents, or another team picking up work (architecture specs,
equipped	kien-thai	AUDIT-PASS	"Write Thai-language prose (technical documentation, marketing copy, explainers, blog posts, translations) that reads like a real Thai writer — not generic AI o
equipped	kode-thai	AUDIT-PASS	Run an iterative audit-and-fix loop on Thai prose using the kien-thai skill — repeats audit/fix passes until a full pass produces zero new edits. TRIGGER when u
equipped	llm-eval-harness	AUDIT-PASS	Use when judging LLM-generated output quality over a DATASET rather than gating a single deliverable — designing input/expected pairs, choosing scorers (heurist
equipped	lowfat-pantry	AUDIT-PASS	Set up and sync the lowfat command-output compactor and its plugin pantry for a project. Use when the user wants to install/configure lowfat, enable token savin
equipped	n8n-troubleshoot	AUDIT-PASS	Use when local n8n 2.37.9 (launchd label com.n8n.local, port 5678) misbehaves — Code node require() failures, missing ExecuteCommand node, workflow import/activ
equipped	po-research-cycle	AUDIT-PASS	Use when acting as Product Owner of the agent-harness memory repo — running one batch research cycle (recall board/topics, pick area, delegate ONE research swar
equipped	qa-gate	AUDIT-PASS	Use when verifying any deliverable before merge, promotion, or equip — shell scripts, required files, commit scope, allowlist .gitignore hygiene, tracked-symlin
equipped	token-build	AUDIT-PASS	Use when the local HTML/Swift dashboards need design tokens — compiles the vendored DTCG token JSON into a single namespace-prefixed CSS variables file (base + 
equipped	web-artifacts-builder	AUDIT-PASS	Suite of tools for creating elaborate, multi-component claude.ai HTML artifacts using modern frontend web technologies (React, Tailwind CSS, shadcn/ui). Use for
equipped	writing-for-agents	AUDIT-PASS	Writing documents for agents. Use when creating or editing skills, or modifying AGENTS.md or CLAUDE.md.
staged	a11y-audit	AUDIT-PASS	Use when auditing agent-built HTML dashboards or UI for accessibility — WCAG 2.2 AA contrast, keyboard navigation, focus visibility, target size, screen-reader 
staged	agent-run-contract	AUDIT-PASS	Use when orchestrating headless agent runs (cron, batch, AFK loops) — designing completion signals, structured-output tags, iteration/timeout policy, or prompt 
staged	apple-silicon-swift	AUDIT-PASS	Use when writing, compiling, or distributing Swift on this Apple Silicon (M1, CLT-only) Mac — choosing zero-dep single-file builds vs frameworks, using the Foun
staged	code-review	AUDIT-PASS	Use when asked to review a diff since a fixed point (commit, branch, tag, HEAD~N), a swarm build lane's output, or a PR before merge. Two-axis review run as par
staged	design-review	AUDIT-PASS	Use when reviewing or scoring agent-built UI — a dashboard, admin screen, or HTML artifact — before it ships. Scores 6 weighted dimensions, applies Nielsen's 10
staged	diagnosing-bugs	AUDIT-PASS	Use when something is broken, failing, throwing, or slow and the cause is not obvious: a Swift dashboard build/runtime failure, an n8n workflow misbehaving, a c
staged	handoff-brief	AUDIT-PASS	Use when ending a session so another agent can continue, swapping a swarm lane under a DELEGATION-CONTRACT, or after a hard-gate rejection that demands a fresh 
staged	llm-eval-harness	AUDIT-PASS	Use when judging LLM-generated output quality over a DATASET rather than gating a single deliverable — designing input/expected pairs, choosing scorers (heurist
staged	n8n-troubleshoot	AUDIT-PASS	Use when local n8n 2.37.9 (launchd label com.n8n.local, port 5678) misbehaves — Code node require() failures, missing ExecuteCommand node, workflow import/activ
staged	po-research-cycle	AUDIT-PASS	Use when acting as Product Owner of the agent-harness memory repo — running one batch research cycle (recall board/topics, pick area, delegate ONE research swar
staged	qa-gate	AUDIT-PASS	Use when verifying any deliverable before merge, promotion, or equip — shell scripts, required files, commit scope, allowlist .gitignore hygiene, tracked-symlin
staged	token-build	AUDIT-PASS	Use when the local HTML/Swift dashboards need design tokens — compiles the vendored DTCG token JSON into a single namespace-prefixed CSS variables file (base + 
tool	a11y-probe	AUDIT-PASS	a11y-probe — static accessibility probe for agent-built HTML (pipeline T) Distilled from the equipped a11y-audit skill knowledge (sources: .agent-harness/tools 
tool	board	AUDIT-PASS	board — kanban API client for the local Swift dashboard engine (pipeline T) Distilled 2026-09-10 from the QA-gated automation-workflow knowledge (sources: .agen
tool	build_tokens_css.py	AUDIT-PASS	Compile DTCG token JSON -> CSS custom properties for the ops dashboards. Distilled from ops/vendor/ux-ui-agent-skills (token-build + design-tokens). Reads the v
tool	n8n-health	AUDIT-PASS	
tool	qa-gate	AUDIT-PASS	
tool	qa-structure	AUDIT-PASS	qa-structure — template-conformance check for research swarm drafts (pipeline T) Distilled from the equipped po-research-cycle skill (QA-gate structure item; so
tool	script-handoff-brief	AUDIT-PASS	usage: script-handoff-brief --name
tool	skills-index	AUDIT-PASS	skills-index — regenerate .agent-harness/SKILLS-INDEX.md (pipeline T4/S4 step) Distilled 2026-09-10 from the skill delegation pipeline v1 (sources: .agent-harne
